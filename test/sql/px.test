# name: test/sql/px.test
# description: test px extension
# group: [px]

# Before we load the extension, this will fail
statement error
SELECT px('Sam');
----
Catalog Error: Scalar Function with name px does not exist!

# Require statement will ensure this test is run with this extension loaded
require px

# Confirm the extension works
query I
SELECT px('Sam');
----
Px Sam 🐥

query I
SELECT px_openssl_version('Michael') ILIKE 'Px Michael, my linked OpenSSL version is OpenSSL%';
----
true
